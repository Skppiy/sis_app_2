import * as React from 'react';
import { Typography } from '@mui/material';

export default function Dashboard() {
  return <Typography>Welcome to SIS. Choose a module from the top navigation.</Typography>;
}
